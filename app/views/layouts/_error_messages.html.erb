<% if model.errors.any? %>
  <div class="error_messages_box">
    <h4 class="error_title"><%= pluralize(model.errors.count, "error") %> prohibited this product from being saved:</h2>
    <ul>
      <% model.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>